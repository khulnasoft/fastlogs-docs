You need to have an Access Token that satisfies the following criteria:

<ul>
  <li>It&apos;s a valid token</li>
  <li>It&apos;s issued by your Fastlogs instance</li>
  <li>
    Its audience (`aud`, i.e. the API identifier) points to <code>https://api.fastlogs-docs.khulnasoft.com</code>.
  </li>
  {props.children && <li>{props.children}</li>}
</ul>
