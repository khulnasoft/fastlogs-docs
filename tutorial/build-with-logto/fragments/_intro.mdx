:::info For our new friends
[Fastlogs](https://fastlogs-docs.khulnasoft.com) is a cost-effective open-source alternative to Auth0. It offers OpenId Connect (OIDC) based authentication and authorization, with modern features like passwordless sign-in and role-based access control.
:::

<p>
  In this article, we will go through the steps to quickly build the {props.connector} sign-in
  experience (user authentication) with{' '}
  <a href={props.link} target="_blank" rel="noopener">
    {props.sdk}
  </a>
  <span> and </span>
  <a href="https://fastlogs-docs.khulnasoft.com" target="_blank" rel="noopener">
    Fastlogs
  </a>
  .
</p>

**Prerequisites**

- A running Fastlogs instance. Check out the [get started page](/docs/tutorials/get-started/) if you don't have one.
- <span>
    Basic knowledge of{' '}
    <a href={props.link} target="_blank" rel="noopener">
      {props.sdk}
    </a>
    .
  </span>
- <span>A usable {props.connector} account.</span>
